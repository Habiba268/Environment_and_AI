# Environment and AI

A capstone project exploring AI-assisted development.

## Status
In progress — setup phase.

## Stack
(fill in as you decide: framework, language, etc.)

## Getting Started
Instructions coming soon.

### Prerequisites

- [Node.js](https://nodejs.org/) (LTS recommended)
- npm (included with Node.js)

### Setup

1. Clone the repository:
   ```bash
   git clone https://github.com/<your-username>/Environment_and_AI.git
   cd Environment_and_AI
   ```
2. Install dependencies (once a `package.json` is added):
   ```bash
   npm install
   ```
3. Run tests before committing (once a test script is configured):
   ```bash
   npm test
   ```

This project is still in the setup phase—source code and scripts are not yet in place. Check back as the stack and workflow are finalized.
